#!/usr/bin/env python3
"""
Authors: Daniel Albuja, Peter Zambrano, Paolo Maldonado,
         J. Riczury Olmos-Tovar, Edwin Vera
Date: 2026-03-28
Description:
    Analyses the optuna_trials.json files generated by optimize_pipeline.py
    for the representative (pilot) barcodes, one per taxonomic group.
    Computes marginal improvement of the objective score and generates
    the convergence plots used in the Supplementary Material.

Usage:
    python analysis/convergencia_optuna_analysis.py --mode GPU
    python analysis/convergencia_optuna_analysis.py --mode CPU

Requirements:
    pandas, numpy, matplotlib, json, pathlib
"""

import argparse
import json
from pathlib import Path

import matplotlib.pyplot as plt
import numpy as np
import pandas as pd

logging.basicConfig(
    format="%(asctime)s [%(levelname)s] %(message)s",
    level=logging.INFO,
)
logger = logging.getLogger(__name__)


# ═══════════════════════════════════════════════════
# USER CONFIGURATION — Modify only this block
# ═══════════════════════════════════════════════════
# Base directory containing the per-barcode results
RESULTS_BASE = Path("/content/drive/MyDrive/results/GPU")   # ← Change to /CPU if needed

# Representative pilot barcodes (one per taxonomic group)
PILOT_BARCODES = {
    "Rhizopus":     "HBAN14_B33",   # Representative for Rhizopus group
    "Aspergillus":  "HPI23_B42",    # Representative for Aspergillus group
    "Cladosporium": "HPIN1_B52",    # Representative for Cladosporium group
}

# Output directory for plots and summary table
OUTPUT_DIR = Path("/content/drive/MyDrive/results")

# Convergence analysis parameters
WINDOW_SIZE = 5          # Rolling window for marginal improvement
EPSILON     = 0.01       # 1 % improvement threshold
# ═══════════════════════════════════════════════════


def load_optuna_history(json_path: Path) -> pd.DataFrame:
    """Load optuna_trials.json and return a sorted DataFrame."""
    with open(json_path) as fh:
        data = json.load(fh)
    df = pd.DataFrame(data)
    df = df[df["value"].notna()].sort_values("trial").reset_index(drop=True)
    return df


def compute_marginal_improvement(df: pd.DataFrame):
    """Compute cumulative best score and smoothed marginal improvement."""
    best_so_far = df["value"].cummin()
    marginal = best_so_far.diff().abs()
    smoothed = marginal.rolling(window=WINDOW_SIZE, min_periods=1).mean()
    return best_so_far, smoothed


def main():
    parser = argparse.ArgumentParser(
        description="CODIGO 4 — Optuna convergence analysis"
    )
    parser.add_argument("--results-base", type=Path, default=RESULTS_BASE,
                        help="Base directory with per-barcode results")
    parser.add_argument("--output-dir",   type=Path, default=OUTPUT_DIR,
                        help="Directory where plots and summary will be saved")
    parser.add_argument("--mode", choices=["CPU", "GPU"], default="GPU",
                        help="Pipeline mode (for output filename prefix)")
    args = parser.parse_args()

    args.output_dir.mkdir(parents=True, exist_ok=True)

    convergence_data = []

    for group, barcode in PILOT_BARCODES.items():
        json_file = args.results_base / barcode / "VSEARCH" / "optuna_trials.json"

        if not json_file.exists():
            logger.warning("optuna_trials.json not found for %s — skipping", barcode)
            continue

        logger.info("Processing pilot barcode %s (%s)", barcode, group)

        df = load_optuna_history(json_file)
        best_so_far, marginal_smoothed = compute_marginal_improvement(df)

        # Store summary for table
        convergence_data.append({
            "group": group,
            "barcode": barcode,
            "n_trials": len(df),
            "final_best_score": round(best_so_far.iloc[-1], 4),
            "trials_to_convergence": int(
                (marginal_smoothed < EPSILON).idxmax()
                if (marginal_smoothed < EPSILON).any() else len(df)
            )
        })

        # Generate convergence plot
        plt.figure(figsize=(8, 5))
        plt.plot(df["trial"], best_so_far, label="Best score so far", marker="o", linewidth=2)
        plt.plot(df["trial"], marginal_smoothed, label=f"Marginal improvement (window={WINDOW_SIZE})",
                 color="orange", linewidth=2)
        plt.axhline(y=EPSILON, color="red", linestyle="--", label=f"ε = {EPSILON} (1%)")
        plt.xlabel("Trial number")
        plt.ylabel("Objective score (lower = better)")
        plt.title(f"Optuna Convergence — {group} ({barcode})")
        plt.legend()
        plt.grid(True, alpha=0.3)
        plt.tight_layout()

        plot_path = args.output_dir / f"convergence_{group}_{barcode}.png"
        plt.savefig(plot_path, dpi=300)
        plt.close()

        logger.info("   → Plot saved: %s", plot_path)

    # Export summary table (Supplementary Material)
    summary_df = pd.DataFrame(convergence_data)
    summary_path = args.output_dir / f"optuna_convergence_summary_{args.mode}.tsv"
    summary_df.to_csv(summary_path, sep="\t", index=False)

    logger.info("Optuna convergence analysis completed successfully (mode: %s)", args.mode)
    logger.info("   → Summary table: %s", summary_path)


if __name__ == "__main__":
    main()